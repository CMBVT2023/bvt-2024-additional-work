// You can either add the word export before the function or variable declaration or export everything separately


// Export before method.
export function squareNumber(value) {
    return value * value;
}

// Export after method
// export { squareNumber };
