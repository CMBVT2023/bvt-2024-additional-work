// Define a function that takes in a string and returns a boolean, true or false, indicating whether it is a palindrome.

// Define a function that takes in a string
// Index through each character (including dashes and spaces)
// Flip the string and index through comparing the words
// Use if statements to make the comparisons, if the letters match continue comparing, if they don't return false.
// If the index reaches the end of the word and all letters match, return true.

function palindromeChecker(word) {
    let arr = [];
    arr = word.split('')

    // Left off on trying to reverse the array and store it in a new variable.
    
    // return false;
}

let result = palindromeChecker("racecar")
console.log(result)