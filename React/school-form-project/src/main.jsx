import React from 'react'
import ReactDOM from 'react-dom/client'
import StudentForm from './containers/FormInputs'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StudentForm />
  </React.StrictMode>,
)
